/**
 * Student Layout
 *
 * Wrapper for all student routes.
 * Currently a pass-through, but provides structure for future enhancements.
 */

export default function StudentLayout({ children }: { children: React.ReactNode }) {
  return children
}
